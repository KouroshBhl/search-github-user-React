import { users, followers, repos } from '../data'

const reducer = function (state, action) {
  switch (action.type) {
    default:
      return state
  }
}

const initalState = {
  users,
  followers,
  repos,
}

export { reducer, initalState }
